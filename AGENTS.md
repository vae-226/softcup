## Agent skills

### Issue tracker

Issues and PRDs are tracked in GitHub Issues for `vae-226/softcup`, using the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Use the default mattpocock/skills triage label vocabulary. See `docs/agents/triage-labels.md`.

### Domain docs

This repo uses a single-context domain docs layout. See `docs/agents/domain.md`.
