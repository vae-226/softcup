<view class="tabbar">
  <view class="tab-item {{item.activeClass}}" wx:for="{{items}}" wx:key="key" bindtap="handleTap" data-tab="{{item.key}}">
    <image class="tab-icon" src="{{item.displayIcon}}" mode="aspectFit"></image>
    <text>{{item.label}}</text>
  </view>
</view>
